0.2.7
    * Update from upstream for Django 1.8 compatibility.
    * Deal with import move of GenericRelation.
0.2.6
    * Default to a slugify.slugify function if present, to provide
      better i18n support.
0.2.5
    * Better Python 3 compatibility
0.2.4
    * Add unique_with to restrict slug uniqueness to the fields specified
    * Still generate uniqueness lookups when a uniqueness field is empty
0.2.3
    * Current upstream verson
